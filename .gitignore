# Environment variables (API keys)
.env

# Claude Code local settings (may contain permission rules with secrets)
.claude/settings.local.json

# macOS
.DS_Store

# Editor
*.swp
*.swo
*~

# Node (if used later)
node_modules/
